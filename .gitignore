# Logs
*.log
npm-debug.log*
firebase-debug*.log*

# Firebase
.firebase/
firebase/.firebase/
# Deploy staging: app/dist/ is copied here by scripts/deploy.sh — never commit it.
firebase/app/

# Dependencies
node_modules/

# Build output
dist/

# Env — commit only the example
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.claude/

# Native shells — generated by `npx cap add ios|android`. Excluded by default.
# Remove these two lines once you start customizing native code and want it in git.
app/ios/
app/android/
